import { SecretKeyValidatorDirective } from '../secret-key-validator/secret-key-validator.directive';

describe('SecretKeyValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new SecretKeyValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
